"""cms-migration-kit — 動的CMSを静的配信+自営スタックへ移行するキット。

パイプライン: ingest(取り込み)→ classify(分類)→ convert(MD・YAML化)
→ build(生成)→ publish(配信)。問い合わせは inquiry/(様式生成・
受信振り分け)。設計は DESIGN.md が正。
"""

__version__ = "0.1.0"
